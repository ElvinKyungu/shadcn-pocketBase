export interface createUSer {
  username: string;
  email: string;
  name: string;
  avatar: string;
}

export interface creatUserWithGoogle{
  id: string;
  username: string;
  email: string;
  name: string;
  avatar: string;
}